impl Solution {
    pub fn search_insert(nums: Vec<i32>, target: i32) -> i32 {
        let mut i = 0;
        for num in nums.iter() {
            if *num == target {
                return i;
            }
            i += 1;
        }

        

        0
    }
}
